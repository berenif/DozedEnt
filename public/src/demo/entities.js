export function createEntityCollector(optionalHandles, clamp) {
  return () => {
    const entities = [];
    // Obstacles and landmarks disabled in demo - return empty list
    return entities;
  };
}
